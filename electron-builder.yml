name: Upload Windows EXE artifact
  uses: actions/upload-artifact@v4
  with:
    name: PaperForge-Windows
    path: |
      dist/win-unpacked/**/*.exe
      dist/*.exe
      dist/*Setup*.exe
    if-no-files-found: error
