directories:
  output: release               # Puts everything in a clean "release" folder
win:
  target:
    - target: nsis              # Installer
      arch: [x64]
    - target: portable          # Optional: creates a single portable .exe
      arch: [x64]
